services:
  backend-users-api:
    build: ./BackEndService/users_api
    ports:
      - "8080"
    restart: on-failure
    networks:
      - taxarific
  # backend-tax-api:
  #   build: ./BackEndService/tax_api
  #   ports:
  #     - "8081"
  #   restart: on-failure
  #   networks:
  #     - taxarific
  # backend-employee-api:
  #   build: ./BackEndService/employee_api
  #   ports:
  #     - "8082"
  #   restart: on-failure
  #   networks:
  #     - taxarific
  frontend-service:
    build: ./FrontEndService
    ports:
      - "3000"
    restart: on-failure
    networks:
      - taxarific
volumes:
  db_data: {}
networks:
  taxarific:
    driver: bridge
